@model Telerik.Sitefinity.Frontend.Card.Mvc.Models.Card.CardViewModel
@using Telerik.Sitefinity.Frontend.Mvc.Helpers

@*<img class="img-absolute" src="@Model.SelectedSizeUrl" alt="@Model.ImageAlternativeText">
    <div class="row h-100 align-items-center">
        <div class="col-12 content-left">
            <h2>@Html.HtmlSanitize(Model.Heading)</h2>
            <p>
                @Html.HtmlSanitize(Model.Description)
            </p>
            <a href="@Html.UrlSanitize(Model.ActionUrl)" class="btn btn-outline-white mt-4">@Model.ActionName</a>
        </div>
    </div>*@

@if (Model.CssClass == "#000000")
{
    <div class="img-absolute bg-image @Model.CssClass" style="background-image: url('@Model.SelectedSizeUrl')"></div>
    <div class="row full-height align-items-center">
        <div class="col-12 content-on-img text-margin-left">
            <h2 style="color:black">@Html.HtmlSanitize(Model.Heading)</h2>
            <p style="color:black">
                @Html.HtmlSanitize(Model.Description)
            </p>
            <a target="_blank" href="@Html.UrlSanitize(Model.ActionUrl)" class="btn btn-outline-black mt-4">@Model.ActionName</a>
        </div>
    </div>
}
else
{
    <div class="img-absolute bg-image @Model.CssClass" style="background-image: url('@Model.SelectedSizeUrl')"></div>
    <div class="row full-height align-items-center">
        <div class="col-12 content-on-img">
            <h2 style="color:black">@Html.HtmlSanitize(Model.Heading)</h2>
            <p style="color:black">
                @Html.HtmlSanitize(Model.Description)
            </p>
            <a target="_blank" href="@Html.UrlSanitize(Model.ActionUrl)" class="btn btn-outline-black mt-4">@Model.ActionName</a>
        </div>
    </div>
}

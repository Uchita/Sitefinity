@using JXTPortal.Entities;

@if(SessionData.Member != null) {
    @Html.ActionLink(SessionData.Member.Username, "Index", "Account", routeValues: null, htmlAttributes: new { data_icon = "gear" })
}
else {
    @Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { data_icon = "gear" })
}
